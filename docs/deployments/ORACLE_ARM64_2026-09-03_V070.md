# Oracle ARM64 production deployment — v0.7 — 2026-09-03

## Outcome

- Deployed `transit-intelligence` v0.7.0 on the existing Oracle Ampere A1 host.
- Host architecture was confirmed as `aarch64` with 2 OCPUs and 12 GB RAM.
- The existing `.env.production` was preserved byte-for-byte and retained mode `600`.
- `DESTRUCTIVE_RETENTION_ENABLED=false` remains enforced.
- Production preflight passed before the stack was rebuilt.
- PostgreSQL/PostGIS, Valkey, API, and Rio ingestion worker all returned healthy after deployment.
- API readiness confirmed both database and cache connectivity.

## Official Rio GTFS evidence

The production GTFS canary downloaded and fully validated the official Rio feed:

- fetched at: `2026-09-03T12:37:24.678783Z`;
- compressed bytes: `25,467,618`;
- uncompressed bytes: `107,874,694`;
- ZIP members: `12`;
- snapshot SHA-256: `c3656db1f22a81f2571b72105d96a219190ae5c5e426ae76eda35e896caa3176`;
- required GTFS files, CSV headers, decompression limits, CRCs, and paths all passed.

This proves the static network/schedule foundation only. It does not substitute for the realtime ingestion gate.

## Realtime and 24-hour soak state

The official realtime endpoint continued to return HTTP 500 from the production VPS. The explicit contract canary failed with `TransitSourceUnavailable`, as expected for that upstream response.

At the final check:

- vehicle positions: `0`;
- ingestion runs: `3,273` total, all failures;
- persisted records: `0`;
- cached position records: `0`;
- observed contract fingerprints: `0`;
- Valkey contained only the ingestion lease key;
- stack smoke passed database and cache checks but correctly failed `recent_position`.

The persistent `transit-intelligence-soak-watch.service` is enabled and active. It remains armed from `2026-09-02T15:26:36Z`; its 24-hour clock will start only after a genuine successful run persists positions, caches positions, and records a non-null contract fingerprint.

## Object Storage and restore revalidation

- OCI S3-compatible object-store smoke passed with a 4,096-byte upload/download SHA-256 check and cleanup.
- Archive restore check passed for `2026-09-01`.
- Restored Parquet evidence remained `0` rows, `1,519` bytes, SHA-256 `0fa2126d2f8911cf6d4c9b8104a8d676b9a900520941a55f01a62dd87c84d8a8`.

The empty archive is a real pre-ingestion day and proves the storage/manifest/download/Parquet verification path, not live data retention.

## Release artifact

- artifact: `transit-intelligence-v0.7.0-production.tar.gz`;
- bytes: `164,698`;
- SHA-256: `45c0310d99ee9f6891b521a252095006d0b539562b8b5f17bee0fb15cd8d0c9a`;
- local and remote hashes matched before deployment.
