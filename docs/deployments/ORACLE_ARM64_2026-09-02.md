# Oracle ARM64 production deployment — 2026-09-02

## Environment

- Region: `sa-saopaulo-1`
- Shape: Oracle Ampere A1 (`aarch64`), 2 OCPUs, 12 GB RAM
- Operating system: Ubuntu 24.04
- Project root: `/home/ubuntu/apps/transit-intelligence`
- API binding: `127.0.0.1:18000`
- Ingestion metrics binding: `127.0.0.1:9101`
- PostgreSQL and Valkey have no published host ports

## Runtime evidence

- PostgreSQL 17.11 on `aarch64`
- PostGIS 3.6.4
- Valkey 8.1.9
- API readiness: database and cache both ready
- database and cache probes survived container restarts and were removed after
  verification

## Object Storage and restore

- Private Standard bucket: `transit-history-sa-sp-20260902`
- Storage mode: `oci-s3`
- Object-store smoke: 4,096-byte upload, full download, matching SHA-256 and
  cleanup of the smoke object
- Verified archive object:
  `transit-history/rio-smtr-gps/2026/09/2026-09-01.parquet`
- Restore check: 0 rows, 1,519 bytes, SHA-256
  `0fa2126d2f8911cf6d4c9b8104a8d676b9a900520941a55f01a62dd87c84d8a8`
- The zero-row archive represents the real empty pre-deployment day. It proves
  the archive/manifest/download/Parquet verification path, not live ingestion.

## Live-source and soak status

At the final deployment check, `https://dados.mobilidade.rio/gps/sppo`
returned HTTP 500 from both the VPS and Oracle Cloud Shell. The worker remained
healthy and recorded 724 failed cycles, but no positions or contract
fingerprint could honestly be asserted.

`transit-intelligence-soak-watch.service` is enabled and running. It is armed
from `2026-09-02T15:26:36Z` and will start the 24-hour clock only after a cycle
has all of the following evidence:

- `status = success`;
- `persisted_records > 0`;
- `cached_records > 0`;
- a non-null contract fingerprint.

The final gate report will be written to `ops/soak/soak-24h.json`.

## Safety state

`DESTRUCTIVE_RETENTION_ENABLED=false`. No destructive hot-history retention
was enabled during this deployment.
