# Oracle ARM64 production deployment — v0.7.1 — 2026-09-03

## Outcome

- Deployed `transit-intelligence` v0.7.1 on the existing Oracle Ampere A1 host.
- Confirmed `aarch64`, 2 OCPUs, 12 GB RAM, and 25 GB free after ten hours of real ingestion.
- Preserved the production secret values and `.env.production` mode `600`.
- Kept `DESTRUCTIVE_RETENTION_ENABLED=false`.
- Production preflight, build, migrations, API health, worker health, and stack smoke passed.
- Retained a code rollback archive at `/home/ubuntu/apps/backups/transit-intelligence-pre-v071-20260903T1408Z.tar.gz`; it deliberately excludes `.env.production`.

## Realtime source correction

The official Rio endpoint currently requires a bounded query using `dataInicial` and `dataFinal`. The v0.7.1 adapter now bootstraps with a two-minute window, overlaps successful windows by 30 seconds, caps recovery windows at five minutes, and advances its cursor only after a valid response.

The isolated production canary ran before the service rollout and returned:

- fetched at: `2026-09-03T14:09:30Z`;
- received records: `16,537`;
- valid records: `16,537`;
- rejected records: `0`;
- fingerprint: `b8a3775b7ce4415241a1ba377303da6801f6c089d5c26dac8c370871465c1e33`.

## Production evidence

At the final audit near `2026-09-03T23:54Z`:

- all four runtime services were healthy;
- API readiness confirmed PostgreSQL and Valkey;
- stack smoke passed database, cache, and recent-position checks;
- PostGIS contained `4,563,761` vehicle positions;
- the latest stored position was from `2026-09-03T23:51:37Z`;
- Valkey contained `6,819` live keys;
- the five most recent ingestion cycles all succeeded;
- those cycles rejected `0` records, persisted between `7,125` and `7,653` records, and cached between `9,691` and `10,205` records;
- all five cycles observed the same contract fingerprint;
- the PostgreSQL database occupied approximately `2,742 MB`.

## Object Storage and restore

- OCI S3-compatible object-store smoke passed with a temporary 4,096-byte object, full download, SHA-256 verification, and cleanup.
- Archive restore passed for `2026-09-01`.
- The restored pre-ingestion Parquet matched the manifest exactly: `0` rows, `1,519` bytes, SHA-256 `0fa2126d2f8911cf6d4c9b8104a8d676b9a900520941a55f01a62dd87c84d8a8`.

This proves storage and restore mechanics, but not a non-empty historical archive. Destructive retention remains disabled until a completed live archive and restore gate is reviewed explicitly.

## Soak state

The persistent `transit-intelligence-soak-watch.service` is active. Its 24-hour window started automatically on the first qualifying real batch:

`2026-09-03T14:11:27.617762Z`

The gate will evaluate the complete 24-hour window using the v0.7.1 polling interval of 60 seconds. Starting the soak does not authorize retention deletion.

## Release and access hygiene

- release artifact: `transit-intelligence-v0.7.1-production.tar.gz`;
- artifact bytes: `165,476`;
- SHA-256: `af6d4d0e40a39f4776e47e7daefb5315a15a6312d470029d3cbd8de5623a6d33`;
- local, Cloud Shell, and VPS hashes matched before deployment;
- temporary deployment files and duplicate secret backup were removed after validation;
- the temporary Bastion session was deleted;
- the Bastion CIDR allowlist was restored exactly to `200.255.57.98/32` and `152.67.37.196/32`.
